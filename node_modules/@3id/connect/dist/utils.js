import { RPCError } from 'rpc-utils';
// TODO didprovider, auth failed codes?
export const rpcError = (id)=>{
    const rpcError1 = new RPCError(-32401, `3id-connect: Request not authorized`);
    return Object.assign(rpcError1.toObject(), {
        id
    });
};
